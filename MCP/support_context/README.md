# support_context

MCP stdio server for Day33 support assistant demo data.

Tools:

- `get_user(user_id)`
- `get_ticket(ticket_id)`
- `find_user_tickets(user_id, limit=5)`

Reads data from `Day33/data/users.json` and `Day33/data/tickets.json`.
